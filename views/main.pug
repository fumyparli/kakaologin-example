doctype
html
    head
        meta(charset='UTF-8')
        title= title
        meta(name='viewport' content='width=device-width, user-scalable=no')
        meta(http-equiv='X-UA-Compatible' content='IE=edge')
        link(rel='stylesheet' href='/main.css')
    body
        .container
            .profile-wrap
                .profile
                    if user && user.id
                        h1= user.provider
                        .user-name= '안녕하세요! ' + user.nick + '님'
                        a#logout.btn(href='/auth/logout') 로그아웃
                    else
                        a#login.btn(type='submit') APPLE
                        a#kakao.btn(href='/auth/kakao') KAKAO
            footer
                | Made by&nbsp;
                a(href='https://www.github.com/fumyparli' target='_blank') fumyparli
    script.
        if (document.getElementById('ToDo')) {
            document.getElementById('ToDo').addEventListener('ToDo', function (e) {
                //
            });
        };